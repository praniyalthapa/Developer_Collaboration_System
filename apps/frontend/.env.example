# Base URL for the backend API.
# Leave as /api in production (served behind the same origin via nginx).
# For local dev against a separate backend port, use http://localhost:7777/api
VITE_API_BASE_URL=/api
